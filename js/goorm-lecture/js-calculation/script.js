const num1 = 20;
const num2 = 10;

let var1;

// Math Object
val = Math.E;
val = Math.PI;
val = Math.round(2.4); // 반올림
val = Math.ceil(2.4); // 무조건 올림
val = Math.floor(2.8); // 무조건 내림

console.log(var1);
